<!DOCTYPE configuration PUBLIC "-//JHelioviewer//DTD Channel Configuration//EN"
  "conf.dtd">
<configuration>

  <!--
	This section contains the global configuration option,
	like parameters of the Icecast streaming server or some
	paths on the server.
  -->

  <global>

    <!-- Hostname or IP of the Icecast server -->
    <stream-server>localhost</stream-server>

    <!-- Portname of the Icecast server -->
    <stream-port>8000</stream-port>

    <!-- Password for the Icecast server -->
    <stream-pass>changeme</stream-pass>

    <!-- Mime type of the streamed content -->
    <stream-type>video/ogg</stream-type>

    <!-- The base URL, where the streaming pages are installed -->
    <urlbase>http://stream.example.com</urlbase>

    <!-- Path of the JPEG 2000 images -->
    <img-base>/home/gabor/img</img-base>
  </global>
  
  <!--
	One entry describes one streaming channel that can
	have very different properties. The mode parameter
	can be either loop-dir or realtime. The former means
	constantly repeatig the images from one folder
	(i.e. one day) and the latter means always emitting
	the last image that arrived.
  -->
  <entry xml:id="channelId" mode="loop-dir">

    <!-- The mount point for the streaming server and the
	 name of the named pipe that is used for communication
	 of the components -->
    <mount-point>sample.ogg</mount-point>

    <!-- Name of the stream to dispaly for the end user -->
    <name>Sample channel</name>

    <!-- A longer description -->
    <desc>Sample description</desc>

    <!--
	 The source directory of images, if it is realtime or cyclic
	 mode, use the %%DATE%% placeholder for the date to insert.
    -->
    <source>path/to/images</source>

    <!-- Instead of the above path names, you can also use the
	 following tags to specify the source.  For directory
	 looping mode, also specify the date tag:

        <observatory>SOHO</observatory>
        <instrument>EIT</instrument>
        <detector>EIT</detector>
        <measurement>304</measurement>
	<date>2012/09/05</date>
    -->

    <!-- Frame per seconds in the generated video -->
    <fps>4</fps>

    <!-- Resolution of the generated video (1:1 proportion) -->
    <resolution>512</resolution>

    <!-- Number of seconds that one image should be displayed.
	 It should be tuned considering the frequency with
	 which images arrive. Furthermore, if chosen too low
	 the buffer stream will frequently underrun. -->
    <sec-per-img>3</sec-per-img>

    <!-- OPTIONAL: If specified, the specified color palette will
	 be applied to the images sent to the channel. -->
    <palette>SDO_AIA_1600.png</palette>

    <!-- OPTIONAL: If specified, the video will be subtitled
	 with timestamps of the specific images with the
	 timestamp in the specified format. The format
	 specification must follow the rules of date(1). -->
    <dateformat>+%Y-%m-%d %H:%M:%S</dateformat>

    <!-- OPTIONAL: the region setting for Kakadu to crop the
	 image to the specified region of interest. -->
    <region>
      <top>0.5</top>
      <left>0.0</left>
      <height>0.5</height>
      <width>0.5</width>
    </region>

    <!-- OPTIONAL: you can list 4 subchannels here and the streamed
	 video will be tiled and clicking on a tile will load
	 the corresponding subchannel, which can have further
	 subchannels. To refer to the subchannels, specify their
	 xml:id attributes here:

    <subchannels>
      <channel href="sub1"/>
      <channel href="sub2"/>
      <channel href="sub3"/>
      <channel href="sub4"/>
    </subchannels>
    -->
  </entry>
</configuration>
